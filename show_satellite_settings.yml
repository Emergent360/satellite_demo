---

- name: "show satellite settings"
  hosts: all
  gather_facts: no

  tasks:
  - name: "show settings"
    ansible.builtin.stat:
      path: /data
    register: data

  - name: print debug
    ansible.builtin.debug:
      var: data