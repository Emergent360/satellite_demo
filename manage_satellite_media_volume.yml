- name: "Manage satellite media volume"
  hosts: all
  gather_facts: no
    
  tasks:
  - name: get volume info
    ec2_vol_info:
      filters:
        "tag:Name": satellite-media
    register: vol_info

